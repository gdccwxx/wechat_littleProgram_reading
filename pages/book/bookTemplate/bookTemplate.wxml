<template name="bookTemplate">
    <view class="bookContainer" data-title="{{title}}" bindtap="bookDetail">
        <image class="bookImg" src="{{coverImg}}"></image>
        <text class="bookTitle">{{title}}</text>
        <text class="author">{{average}}</text>
    </view>
</template>